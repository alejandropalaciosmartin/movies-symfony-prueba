{% extends "base.html.twig" %}

{% block title %}Genres{% endblock %}

{% block body %}
    <div class="bg-gray-900 py-16">
        <div class="container mx-auto px-4">
            <h1 class="text-6xl text-center text-white">
                <i class="fa-brands fa-typo3" style="color: #ffffff;"></i> Genres <i class="fa-brands fa-typo3" style="color: #ffffff;"></i>
            </h1>
        </div>
    </div>

    <div class="container mx-auto px-4 py-8">
        <div class="flex justify-center">
            <div class="bg-white rounded-lg shadow-lg overflow-hidden relative">
                <div class="px-6 py-4">

                    <!-- Add Genre Button -->
                    <a href="{{ path('app_genre_create') }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mb-4 flex items-center justify-center">
                        <i class="fa-solid fa-plus" style="color: #ffffff;"></i>Add Genre
                    </a>
                    {% if genres is empty %}
                        <p class="text-gray-700 text-base">There are no genres in the database.
                            <a href="#" class="text-blue-500">Add one</a>!
                        </p>
                    {% else %}
                        <ul>
                            {% for genre in genres %}
                                <li class="flex justify-between items-center mb-6">
                                    <span>{{ genre.name }}</span>

                                    <!-- Delete Genre Button -->
                                    <a href="{{ path('app_genre_delete', {'id': genre.id}) }}" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
                                        <i class="fas fa-trash"></i> Delete
                                    </a>
                                </li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}